# ============================================
# MCP Prompt Manager - Environment Variables
# ============================================

# Storage Driver Type
# Options: github, local, memory, s3
# Default: local
PROMPT_STORAGE=local

# ============================================
# Local Git Storage (PROMPT_STORAGE=local)
# ============================================
# Local Git repository path (optional, default: .prompts_cache)
# Can be absolute path or relative path (relative to current working directory)
STORAGE_DIR=/path/to/your/local/git/repo
# Or use relative path:
# STORAGE_DIR=./my-prompts

# ============================================
# GitHub Storage (PROMPT_STORAGE=github)
# ============================================
# Git Repository URL (required for github storage type)
# Local path example:
# PROMPT_REPO_URL=/Users/yourname/Desktop/my-local-prompts
# Remote Git URL examples:
# PROMPT_REPO_URL=https://github.com/yourusername/my-prompts.git
# PROMPT_REPO_URL=git@github.com:yourusername/my-prompts.git

# Storage directory (optional, default: .prompts_cache)
STORAGE_DIR=.prompts_cache

# Git branch (optional, default: main)
GIT_BRANCH=main

# Git max retries (optional, default: 3)
GIT_MAX_RETRIES=3

# ============================================
# Memory Storage (PROMPT_STORAGE=memory)
# ============================================
# No additional configuration needed
# Used for testing or dynamic loading

# ============================================
# S3 Storage (PROMPT_STORAGE=s3)
# ============================================
# S3 bucket name (required for s3 storage type)
S3_BUCKET_NAME=my-prompts-bucket

# S3 region (optional, default: us-east-1)
S3_REGION=us-west-2

# Method 1: SDK Mode (supports private buckets)
# If both S3_ACCESS_KEY_ID and S3_SECRET_ACCESS_KEY are provided, SDK mode will be used
S3_ACCESS_KEY_ID=your-access-key-id
S3_SECRET_ACCESS_KEY=your-secret-access-key

# Method 2: URL Mode (public buckets)
# If credentials are not provided, URL mode will be used automatically
# S3_BASE_URL=https://my-prompts-bucket.s3.amazonaws.com

# S3 object prefix (optional, used to limit scan scope)
# S3_PREFIX=prompts/common

# ============================================
# General Settings
# ============================================
# Output language (optional, default: en)
# Options: en, zh
MCP_LANGUAGE=en

# Group filter (optional, default: common)
# When not set, only 'common' group will be loaded (system will log a hint)
# Example: MCP_GROUPS="laravel,vue,react"
MCP_GROUPS=laravel,vue

# Log level (optional)
# Options: fatal, error, warn, info, debug, trace, silent
# Default: warn (production) / info (development)
LOG_LEVEL=info

# Log file path (optional, strongly recommended)
# When set, all level logs will be written to file (JSON format)
LOG_FILE=logs/mcp.log
