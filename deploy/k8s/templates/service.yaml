apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-prompt-manager.fullname" . }}
  labels:
    {{- include "mcp-prompt-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-prompt-manager.selectorLabels" . | nindent 4 }}
